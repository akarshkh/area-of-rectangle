//write a c program to enter length and breadth of rectangle and find its area.
#include<stdio.h>
int main()
{
   int l,b;
    printf("Enter the value of l in cm\n");
    scanf("%d",&l);
    printf("Enter the value of b in cm\n");
    scanf("%d",&b);
    l*b;
    printf("the area of rectangle is %dcm",l*b);
    
    return 0;

}
